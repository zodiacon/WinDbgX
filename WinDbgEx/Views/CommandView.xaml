<UserControl x:Class="WinDbgEx.Views.CommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinDbgEx.Views"
             xmlns:c="clr-namespace:WinDbgEx.UICore.Controls;assembly=WinDbgEx.UICore"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <c:CommandResultHistory />
        <DockPanel Grid.Row="1">
            <Button Content="Execute" DockPanel.Dock="Right" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Prompt}" DockPanel.Dock="Left" VerticalAlignment="Center" />
            <TextBox Text="{Binding CommandText}" Margin="4,0" />
        </DockPanel>
    </Grid>
</UserControl>
